import React from "react";

const SideNav = (props) => {
  return (
    <div className="SideNav">
        <a>All Users</a>
        <a>Create User</a>
        <a>Update User</a>
        <a>Delete User</a>
    </div>
  );
};

export default SideNav;
